<form method="post" action="{{ path('app_venta_delete', {'id': ventum.id}) }}" style="display:inline-block;" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ventum.id) }}">
    <button class="btn btn-danger btn-sm">Eliminar</button>
</form>
